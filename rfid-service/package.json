{
  "name": "coaching-os-rfid-service",
  "version": "1.0.0",
  "description": "CoachingOS RFID Service — MQTT subscriber for RFID hardware",
  "private": true,
  "scripts": {
    "prebuild": "rimraf dist",
    "build": "tsc",
    "start": "node dist/main.js",
    "start:dev": "ts-node-dev --respawn src/main.ts",
    "lint": "eslint 'src/**/*.ts' --fix"
  },
  "dependencies": {
    "mqtt": "^5.3.4",
    "nats": "^2.19.0",
    "pg": "^8.11.3",
    "dotenv": "^16.3.1",
    "prom-client": "^15.1.0",
    "@opentelemetry/auto-instrumentations-node": "^0.40.0",
    "@opentelemetry/exporter-trace-otlp-grpc": "^0.45.0",
    "@opentelemetry/sdk-node": "^0.45.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/pg": "^8.10.9",
    "rimraf": "^5.0.5",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.3"
  }
}
